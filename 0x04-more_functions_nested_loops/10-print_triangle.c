#include "main.h"

/**
 * print_triangle - prints a triangle.
 * @n: numbers of lines.
 * Return: no return.
 */
void print_triangle(int n)
{
	int i, j;

	for (i = 0; i < n; i++)
		if (i < (n - 1))
			_putchar('\n');
	}
	_putchar('\n');
}
